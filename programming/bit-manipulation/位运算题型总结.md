# 位运算题型总结

- 3370. 仅含置位位的最小整数  ： 求二进制宽度 bit_widty
- 3226. 使两个整数相等的位更改次数 ： 集合的思想 n & k == k , k 属于n  
- 1356. 根据数字二进制下 1 的数目排序 : 二进制1的个数动态规划 bit[i] == bit[i>>1] + (i&1)
- 461. 汉明距离 : 异或求二进制1的个数 ___builtin_popcount(a^b)
- 476. 数字的补数: 


与或运算
- 2980. 检查按位或是否存在尾随零, 只需要判断奇偶即可，注意位运算优先级问题
- 1318. 或运算的最小翻转次数 
- 2419. 按位与最大的最长子数组: 变相求最大值的最长子数组，一次遍历,需要会 



log trick
- 3171. 找到按位或最接近 K 的子数组 : 子数组，逆序，提取停止
- 1521. 找到最接近目标值的函数值 同 3171 题，OR 改成 AND
- 3097. 或值至少为 K 的最短子数组 II 


异或的性质
a^a = 0, a^0 = a
- 136. 只出现一次的数字, 将所有数异或，结果就是只出现一次的数字